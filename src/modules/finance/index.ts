import { CardsModule } from './cards';
import { TransactionsModule } from './transactions';
import { WalletsModule } from './wallets';

export const FINANCE_MODULES = [CardsModule, TransactionsModule, WalletsModule];
