export enum JitGatewayWebhookEvent {
  CARD_BALANCE = 'card.balance',
  AUTHORIZATION_REQUEST = 'authorization.request',
  AUTHORIZATION_DECLINED = 'authorization.declined',
  TRANSACTION_CREATED = 'transaction.created',
}
